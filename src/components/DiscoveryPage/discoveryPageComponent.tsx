import { FC, useState } from 'react';

interface DiscoveryPageProps {
  activities: Array<Activity>;
}

const DiscoveryPage: FC<DiscoveryPageProps> = ({ activities = [] }) => {
  return <div></div>;
};
